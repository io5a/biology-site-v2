/**
 * Theme Configuration
 * 
 * Change the theme by modifying the 'theme' value below.
 * Available themes: 'default', 'blue', 'purple', 'orange', 'red', 'pink', 'cyan'
 */

export const themeConfig = {
  theme: 'default' as 'default' | 'blue' | 'purple' | 'orange' | 'red' | 'pink' | 'cyan',
}
